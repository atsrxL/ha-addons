name: Raritan PX MQTT
version: 0.2.1
slug: raritan_px_mqtt
description: Bridge a Raritan PX2/PX3 PDU to Home Assistant through the existing MQTT broker.
url: https://github.com/atsrxL/ha-addons/tree/main/raritan_px_mqtt
arch:
  - aarch64
  - amd64
init: false
startup: services
boot: auto
stage: experimental
services:
  - mqtt:need
options:
  pdu_host: 192.168.123.78
  pdu_username: admin
  pdu_password: ""
  protocol: https
  verify_ssl: false
  poll_interval: 15
  discovery_prefix: homeassistant
  topic_prefix: raritan2mqtt
  log_level: INFO
schema:
  pdu_host: str
  pdu_username: str
  pdu_password: password
  protocol: list(http|https)
  verify_ssl: bool
  poll_interval: int(5,3600)
  discovery_prefix: str
  topic_prefix: str
  log_level: list(DEBUG|INFO|WARNING|ERROR)