name: Build Mercury PoE MQTT

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - "mercury_poe_mqtt/**"
      - ".github/workflows/build-mercury-poe-mqtt.yaml"
  push:
    branches:
      - main
    paths:
      - "mercury_poe_mqtt/**"
      - ".github/workflows/build-mercury-poe-mqtt.yaml"

concurrency:
  group: mercury-poe-mqtt-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

jobs:
  build:
    name: Build multi-architecture image
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Read app version
        id: app
        shell: bash
        run: |
          version="$(awk -F': *' '$1 == "version" {gsub(/"/, "", $2); print $2; exit}' mercury_poe_mqtt/config.yaml)"
          test -n "${version}"
          echo "version=${version}" >> "${GITHUB_OUTPUT}"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and publish image
        uses: docker/build-push-action@v6
        with:
          context: ./mercury_poe_mqtt
          platforms: linux/amd64,linux/arm64
          push: ${{ github.event_name != 'pull_request' }}
          tags: |
            ghcr.io/atsrxl/ha-addon-mercury-poe-mqtt:${{ steps.app.outputs.version }}
            ghcr.io/atsrxl/ha-addon-mercury-poe-mqtt:latest
          build-args: |
            BUILD_VERSION=${{ steps.app.outputs.version }}
          cache-from: type=gha,scope=ha-addon-mercury-poe-mqtt
          cache-to: type=gha,mode=max,scope=ha-addon-mercury-poe-mqtt
          provenance: false
