name: Build Raritan PX MQTT

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - "raritan_px_mqtt/**"
      - ".github/workflows/build-raritan-px-mqtt.yaml"
  push:
    branches:
      - main
    paths:
      - "raritan_px_mqtt/**"
      - ".github/workflows/build-raritan-px-mqtt.yaml"

concurrency:
  group: raritan-px-mqtt-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

jobs:
  build:
    name: Build multi-architecture image
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Read app version
        id: app
        shell: bash
        run: |
          version="$(awk -F': *' '$1 == "version" {gsub(/"/, "", $2); print $2; exit}' raritan_px_mqtt/config.yaml)"
          test -n "${version}"
          echo "version=${version}" >> "${GITHUB_OUTPUT}"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and publish image
        uses: docker/build-push-action@v6
        with:
          context: ./raritan_px_mqtt
          platforms: linux/amd64,linux/arm64
          push: ${{ github.event_name != 'pull_request' }}
          tags: |
            ghcr.io/atsrxl/raritan-px-mqtt:${{ steps.app.outputs.version }}
            ghcr.io/atsrxl/raritan-px-mqtt:latest
          build-args: |
            BUILD_VERSION=${{ steps.app.outputs.version }}
          cache-from: type=gha,scope=raritan-px-mqtt
          cache-to: type=gha,mode=max,scope=raritan-px-mqtt
          provenance: false
